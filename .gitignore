# Исходные фото — НЕ публиковать (среди них есть кадры с людьми)
assets/source/

# Разовый инструмент генерации иконок, в приложении не нужен
make-icons.html

# Локальная конфигурация Claude Code
.claude/

# Прочее
.DS_Store
Thumbs.db

# Локальная проверка миграций хранилища (харнесс + синтетический фикстур).
# fixture-real*.json НЕ коммитить — там реальные данные о питомце.
dev/
